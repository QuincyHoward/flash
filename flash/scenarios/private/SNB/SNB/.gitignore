# ════════════════════════════════════════════════════════════════
# SNB 分享区本地二次防护 (docs/06_分享与保密规则.md)
#
# 本目录**只承载可分享内容**：文档 / 参数 / 驱动·分析·绘图脚本。
# 但本目录同时是 SNB 场景处理的**核心本地模块** —— 因此下列
# 「本地保留层」必须存在但**不得入库**。
# ════════════════════════════════════════════════════════════════

# ── 0. ★ 本地保留层 (存在但不分享) ─────────────────────────────
#   source/       作者 SNB 实现包 + 参考版 + EOS/opacity 表 + 示例 par
#   variants/     F90 诊断变体 (由作者源码派生)
#   tools_local/  会**写入/生成 F90** 的脚本 (脚本即等价于源码)
#   以上三者为 SNB 工作流所必需, 但受分享规则与 FLASH License §3 约束。
source/
variants/
tools_local/

# ── 1. F90 源码与派生变体（分享规则的硬红线） ──────────────────
*.F90
*.f90
*.F
*.f
**/variants/

# ── 2. 会话/场景运行产物 ───────────────────────────────────────
flash_input/
flash_output/
**/flash_input/
**/flash_output/
*.hdf5*
*_hdf5_chk_*
*_hdf5_plt_cnt_*
*.log
*.dat

# ── 3. EOS/opacity 表（二进制大数据；是否分享另行决定） ─────────
*.cn4

# ── 4. 凭据 / 密钥 / 账号 ─────────────────────────────────────
*hpc_accounts*
*credentials*
*.pem
*.key
.env
.env.*

# ── 5. 编译产物 / 缓存 ────────────────────────────────────────
*.o
*.a
*.so
*.mod
*.pyc
__pycache__/
.pytest_cache/

# ── 6. 临时/备份 ──────────────────────────────────────────────
*~
*.bak
*.tmp
*副本*
temp_delete/
